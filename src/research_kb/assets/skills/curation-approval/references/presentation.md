# Proposal presentation

Show proposal identifier and expected revision, then a numbered convenience list. Each item includes stable decision identifier, action, concise reason, and pending/accepted/rejected state. Include affected records and protected fields left unchanged. End with the exact `research curation decide` command returned by the service, which the user must run. Later reversals use the separately confirmed `research curation undo <operation-id>` command.
